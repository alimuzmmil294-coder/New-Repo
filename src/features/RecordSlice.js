import { createSlice } from "@reduxjs/toolkit";

const recordSlice = createSlice({
  name: "EmployeeRecord",
  initialState: {},
  reducers: {},
});

const {} = recordSlice.actions;

export default recordSlice.reducer;
